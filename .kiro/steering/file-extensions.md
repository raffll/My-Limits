# File Extensions

Never use `.omwscripts.json` as a file extension. Use plain `.json` instead.

`.omwscripts.esp` is a special extension used by Mod Organizer 2's OpenMW plugin. It tells MO2 to treat the file as an omwscripts content entry rather than a regular ESP. Do not rename or delete these files.
