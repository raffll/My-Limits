# Frame Tolerance

One-frame visual glitches or state inconsistencies are acceptable. Do not add workarounds or extra logic to prevent single-frame artifacts unless the user explicitly reports a visible flicker or problem.
